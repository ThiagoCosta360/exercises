const tickets = (peopleInLine) => peopleInLine.array.forEach(element => {
    
});((t,c) => c === 25 ? t+25 : t-(c-25),0) > 0 ? "YES" : "N0"

console.log(tickets([25, 25]))

// console.log([25,25].reduce((t,c) => c === 25 ? t+25 : t-(c-25),0))